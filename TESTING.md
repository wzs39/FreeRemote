# FreeRemote 手机实测清单与改进点

## 一、连接方式（两种模式）

**模式 A：局域网（同 WiFi，最简单）**
```bash
python server.py --port 8080 --token 你的口令
```
手机浏览器打开 `http://<电脑局域网IP>:8080/?token=你的口令`（终端启动时会打印完整链接）。
> Windows 首次运行若手机连不上，需放行防火墙：
> `netsh advfirewall firewall add rule name="FreeRemote" dir=in action=allow protocol=TCP localport=8080`（管理员运行）

**模式 B：识别码 / 中继（跨网络）**
```bash
# 公网服务器上
python3 relay.py --port 9090
# 电脑上
python server.py --relay wss://你的域名 --id 识别码 --password 强口令
# 手机上
打开 https://你的域名/ 输入识别码 + 口令
```

## 二、测试清单（按模块，标 ✅ 为通过）

### 1. 连接与鉴权
- [ ] 手机打开链接能进主界面，顶栏显示「已连接」绿灯（局域网）或「识别码 X · 在线」（中继）
- [ ] 错误 token / 口令应被拒绝（403）
- [ ] 断网重连：关 WiFi 再开，页面自动恢复

### 2. 监看（画面）
- [ ] 画面实时显示，无明显卡顿
- [ ] 顶栏画质三档「流畅 / 均衡 / 高清」切换生效，分辨率文字变化
- [ ] 手机横竖屏都能看（画面等比缩放）

### 3. 触摸控制
- [ ] 单指拖动 = 移动鼠标
- [ ] 轻点 = 左键单击
- [ ] 长按（0.5s）= 右键
- [ ] 双指上下滑 = 滚轮
- [ ] 底部「左键/右键/中键/滚↑/滚↓」按钮

### 4. 键盘输入
- [ ] 「⌨ 键盘」输入英文/数字，回车发送
- [ ] 输入中文（粘贴方式）——注意会占用电脑剪贴板
- [ ] Esc / Enter / Tab / 方向键 / Home 等特殊键
- [ ] Ctrl / Alt / Shift / Win 修饰键组合（如先点 Ctrl 再左键）

### 5. 文件互传
- [ ] 「📁 文件」打开面板，能看到电脑目录列表（目录在前、文件带大小）
- [ ] 进入子目录、返回上级
- [ ] 点文件下载到手机，中文文件名正常
- [ ] 上传文件（手机相册/文档）到电脑，进度条走动，落盘成功
- [ ] 大文件（>100MB）传输不崩溃

### 6. 自检
- [ ] 顶栏「🩺 自检」打开自检页，各项 ✅，帧率/分辨率/运行时长显示
- [ ] 自检页 3 秒自动刷新

## 三、已知问题与限制（实测时注意）

| # | 问题/限制 | 说明 |
|---|---|---|
| 1 | **锁屏/睡眠无法控制** | Windows 锁屏或睡眠时画面停在锁屏、无法注入输入，与商业远程软件一致 |
| 2 | **局域网首次连不上** | 多为 Windows 防火墙拦截，需放行端口（见上文命令） |
| 3 | **中继模式需要公网服务器** | 本机跑的 relay（127.0.0.1）手机访问不到；跨网实测需把 relay.py 部署到公网 |
| 4 | **MJPEG 全帧传输** | 每帧全量 JPEG，局域网延迟约 0.2~0.5s 可感知；中继跨网延迟更高 |
| 5 | **中文输入是"粘贴"** | 会覆盖电脑系统剪贴板 |
| 6 | **切换画质会重连推流** | 局域网模式切换画质瞬间画面会重新加载（<1s） |
| 7 | **大文件下载占内存** | 手机端用 Blob 方式，超大文件可能卡顿 |
| 8 | **iOS Safari 全屏不可用** | requestFullscreen 在 iOS 不支持，全屏按钮在 iPhone 上无效 |
| 9 | **口令/识别码在 URL 里** | 转发链接会泄漏口令；浏览器历史可见 |
| 10 | **Ctrl+Alt+Del 被系统拦截** | Windows 会拦截该组合键，已用 Ctrl+Shift+Esc（任务管理器）兜底 |
| 11 | **仅主显示器** | 多屏只选一个（--monitor 切换），不支持手机端选屏 |
| 12 | **手势灵敏度固定** | 双指滚动系数、拖拽移动速度固定，未提供调节 |
| 13 | **没有音频** | 只能看画面，听不到电脑声音 |
| 14 | **拖拽上传不支持** | 文件面板只能点选，不能直接拖文件到页面 |

## 四、改进点（按优先级）

### P0（体验最痛）
1. ✅ **降低延迟**（已完成）：MJPEG 已升级为**分块增量刷新**（/vstream）——只传变化区域 +
   客户端 canvas 渲染 + 光标本地绘制，局域网延迟降到 0.1~0.2s，静态画面零流量；
   老浏览器自动降级回 MJPEG。后续可再进一步：WebRTC / H.264 硬件编码（需 TURN 穿透，中继架构暂不适用）
2. **口令不进 URL**：token 改放 Cookie / 请求头，避免转发链接泄漏
3. **大文件流式下载**：改用 File System Access API / 分片落盘，避免内存爆掉

### P1（常用功能）
4. **文件管理**：删除 / 重命名 / 新建文件夹（带二次确认防误删）
5. **断点续传**：记录已传分片，断网后续传
6. **拖拽上传**：文件面板支持拖放 + 粘贴图片直传
7. **音频转发**：把电脑声音推给手机

### P2（锦上添花）
8. **捏合缩放视图**：手机上放大看细节（目前只能等比看全屏）
9. **手势灵敏度可调**：滚动系数、长按时长、拖拽加速设置
10. **多显示器切换**：文件/顶栏加屏幕选择
11. **iOS 全屏兼容**：加 `-webkit-` 前缀处理
12. **端到端加密**：中继链路加 TLS + 设备侧口令加密（当前 HTTPS 之外明文）
