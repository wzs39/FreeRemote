# 复制为 .env 后填写（.env 不要提交到 Git，里面是密钥）

# 公网 HTTPS 版必填：你的域名（已解析到本服务器 A 记录）
DOMAIN=relay.example.com

# 预注册的设备：识别码:强口令（可用英文逗号加多个：TEST01:pass1,TEST02:pass2）
# 口令建议 >= 12 位，服务器端有防暴力破解限速
RELAY_DEVICES=TEST01:change-me-to-a-strong-password

# 1 = 只允许上面登记的识别码连接（推荐）；0 = 允许任意识别码自动注册
RELAY_STRICT=1
